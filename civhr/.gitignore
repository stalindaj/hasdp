*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
# NOTE: /public/build is deliberately NOT ignored. The cPanel host has no Node,
# so assets are built locally (`npm run build`) and committed, then pulled on
# the server. If you ever move to a host with Node, re-add /public/build here
# and build during deploy instead.
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db
